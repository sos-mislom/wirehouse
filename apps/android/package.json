{
  "name": "@warehouse/android",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:web": "cd ../.. && npm --workspace apps/web run build",
    "android:add": "npx cap add android",
    "android:sync": "node ./scripts/render-capacitor-config.mjs && npm run build:web && npx cap sync android",
    "android:open": "npx cap open android",
    "android:release": "node ../../scripts/check-release-env.mjs android && node ./scripts/render-capacitor-config.mjs && npm run build:web && npx cap sync android && node ./scripts/prepare-release-signing.mjs && node ./scripts/build-release.mjs"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.3",
    "@capacitor/core": "^7.4.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.3",
    "typescript": "^5.9.3"
  }
}
