# Результаты проверки · 16 сентября 2026

- `npm run check`: TypeScript без ошибок, 9 тестов данных/API успешно, Vite production build успешно.
- `npm run test:browser`: Chromium; переходы и Назад, отмена редактирования, оборудование, ППР, канбан, пользователь, план с отметкой у края, кабинеты арендатора и исполнителя, показания счётчика.
- Локальная мобильная матрица 320 / 360 / 390 / 768 / 1024 px: 12 основных административных экранов на каждой ширине; отдельные проверки кабинетов арендатора/исполнителя на 320 px. Горизонтального переполнения документа нет.
- Опубликованный HTTPS: реальный вход по паролю, 12 экранов × 4 ширины (320 / 390 / 768 / 1024), бургер и Escape; без ошибок JavaScript и CSP.
- `npm audit --omit=dev --workspace apps/api --workspace apps/web --workspace packages/contracts --include-workspace-root`: 0 известных уязвимостей.
- `git diff --check`: без ошибок.
- PostgreSQL и API healthy, API открыт только на loopback хоста.
- `certbot renew --cert-name wirehouse.92.118.115.96.nip.io --dry-run`: тестовое продление успешно.
- `nginx -t`: успешно. Контрольные суммы всех 12 существовавших файлов Nginx совпадают с исходными.
- Прежние ответы сайтов сохранены: yondex.tech, golos-goroda.ru, ekb.golos-goroda.ru, a.whyfi.ru, hykz.92.118.115.96.nip.io — 200; www.yondex.tech — исходный 500.
- Резервная копия проверена SHA256, затем восстановлена через pg_restore в отдельную временную базу. Найдены 8 пользователей и 2 объекта; проверочная база удалена, основная не менялась.
- Запуск `wirehouse-backup.service`: Result=success, ExecMainStatus=0; ежедневный timer активен.

Тестовые данные из автоматических сценариев изолированы во временных локальных базах; серверный портфель этими сценариями не изменяется. Публичная браузерная проверка только читает разделы и обновляет время входа администратора.

Автоматические проверки не доказывают отсутствие всех ошибок. Ограничения инфраструктуры и интеграций перечислены в [DEPLOYMENT.md](DEPLOYMENT.md) и [REVIEW.md](REVIEW.md).
